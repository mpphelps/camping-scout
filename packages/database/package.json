{
  "name": "@campingmeow/database",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "main": "src/client.ts",
  "scripts": {
    "generate": "prisma generate",
    "migrate": "prisma migrate dev",
    "migrate:deploy": "prisma migrate deploy"
  },
  "dependencies": {
    "@prisma/adapter-pg": "^7.6.0",
    "@prisma/client": "^7.6.0"
  },
  "devDependencies": {
    "dotenv": "^16.0.3",
    "prisma": "^7.6.0"
  }
}
